# GitHubPractice01

1. Download the files 04+Practice+Project+(Start).zip and turn it into a local git repository.
	Unzip the files in a new project.
	git init -> to initialize and create a git Repository.
	git add . -> add all files to commit
	git commit -m "Initial commit" -> create first commit with -m message
	git log -> check logs about commit
	clear -> clean terminal screen
2. Create a GitHub repository for your downloaded exercise file (remote repository).
	login into GitHub and create a new repo for your new project.
3. And connect git to the GitHub remote repository.
4. In that GitHub repository create 2 workflows:
	- Lint, test and deploy on push (download the project code when is triggerd, install any dependency, run lint, test and build)
	- 
